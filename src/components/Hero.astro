<section id="hero" class="content-section initial-visible">
	<div class="hero-content">
		<pre>Dane Gibson</pre>
		<h1>
			<strong>Full-Stack Developer</strong>
		</h1>
	</div>
	<div class="scroll-down-indicator" aria-hidden="true">
		<span></span>
	</div>
</section>

<style>
	#hero {
		min-height: 100vh;
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		text-align: center;
		margin-bottom: 60px;
		padding-bottom: 80px;
		position: relative;
		opacity: 1;
		transform: none;
		transition: none;
	}

	@keyframes fadeInUp {
		from {
			opacity: 0;
			transform: translateY(20px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	.hero-content pre,
	.hero-content h1 {
		opacity: 0;
		animation: fadeInUp 0.8s ease-out forwards;
	}

	.hero-content pre {
		animation-delay: 0.3s;
		font-family: 'Roboto Mono', 'Cascadia Code', 'Source Code Pro', Menlo,
			Consolas, 'DejaVu Sans Mono', monospace;
		font-weight: normal;
		background: linear-gradient(14deg, #d83333 0%, #f041ff 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin: 0;
		font-size: clamp(1.8rem, 4vw, 2.5rem);
	}

	.hero-content h1 {
		animation-delay: 0.5s;
		font-size: clamp(2.2rem, 6vw, 4rem);
		margin-top: 0.2em;
		margin-bottom: 0;
		font-weight: 700;
		color: var(--text-primary);
	}

	@keyframes bounce {
		0%,
		20%,
		50%,
		80%,
		100% {
			transform: translateY(0);
		}
		40% {
			transform: translateY(-15px);
		}
		60% {
			transform: translateY(-8px);
		}
	}

	.scroll-down-indicator {
		position: absolute;
		bottom: 80px;
		left: 50%;
		transform: translateX(-50%);
		width: 24px;
		height: 40px;
		border: 2px solid var(--text-primary);
		border-radius: 12px;
		opacity: 0;
		animation: fadeInUp 0.8s 1s ease-out forwards;
	}

	.scroll-down-indicator span {
		position: absolute;
		top: 8px;
		left: 50%;
		transform: translateX(-50%);
		width: 4px;
		height: 8px;
		background-color: var(--text-primary);
		border-radius: 2px;
		animation: bounce 2s infinite ease-out 1.5s;
	}

	@media (prefers-reduced-motion: reduce) {
		.hero-content pre,
		.hero-content h1,
		.scroll-down-indicator {
			animation: none !important;
			opacity: 1 !important;
			transform: none !important;
		}
	}

	@media screen and (max-width: 768px) {
		#hero {
			min-height: 90vh;
			padding-bottom: 60px;
		}
		.hero-content h1 {
			font-size: clamp(2rem, 10vw, 3rem);
		}
		.hero-content pre {
			font-size: clamp(1.6rem, 7vw, 2.2rem);
		}
        .scroll-down-indicator {
            bottom: 60px;
        }
	}
</style>
